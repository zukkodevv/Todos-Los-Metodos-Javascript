//* En este ejemplo veremos más a profundidad el método lenght
//* en un objeto.

const elementos = { hidrogeno: "agua", oxigeno: "aire", cobre: "mineral" };
// Definimos un objeto constante con 3 elementos con sus respectivos valores

console.log(Object.keys(elementos).length); // 3
// Le indicamos al código que imprima en consola el resultado de el constructor objeto,
// que crea un nuevo objeto y con el método keys le introduce los elementos del
// objeto entre paréntesis, en este caso; (elementos) y con el método lenght imprime su longitud.
